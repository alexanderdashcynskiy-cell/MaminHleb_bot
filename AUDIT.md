# 🔍 АУДИТ КОДА — МАМИН ХЛЕБ

**Дата:** 17 мая 2026  
**Роль аудитора:** Senior Frontend/Backend Developer, UI/UX Designer, Cybersecurity Specialist (12 лет опыта)  
**Проекты:** 2 репозитория  
**Итоговое число проблем:** 75 (46 CRM + 24 Bot + 5 Cross-project)  
**Метод:** Полное чтение всех исходных файлов без исключений  
**Ветка:** `claude/audit-white-fox-dashboard-W4DAS`

---

## Условные обозначения

| Символ | Значение |
|--------|----------|
| 🔴 | Критическая проблема — требует немедленного исправления |
| 🔐 | Безопасность — уязвимость или риск утечки данных |
| 🐛 | Баг / логическая ошибка |
| 🎨 | UI/UX проблема |
| 📦 | Проблема с данными |
| 🚀 | Деплой / сборка |
| 💀 | Мёртвый код |
| 📝 | Архитектура |
| ✅ | Исправлено |

---

---

# ПРОЕКТ 1: `crm-maminhleb7`

**Стек:** React 19 + TypeScript + Vite 6 + Express.js + Google Sheets API + Gemini AI + Tailwind CSS v4 + shadcn/ui  
**Деплой:** Railway.app  
**Telegram Mini App:** Да

---

## 🔴 Критические проблемы (5)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `vite.config.ts` | 11 | `GEMINI_API_KEY` бакается в клиентский бандл через `define: { 'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY) }` — ключ виден в DevTools → Sources у любого пользователя |
| 2 | `src/App.tsx` | 108, 614 | `import { GoogleGenAI }` и `new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY })` — Gemini SDK инициализируется прямо в браузере; ключ утекает в бандл и сетевой трафик |
| 3 | `src/App.tsx` | 2091, 2143, 2250, 2395, 2435, 2471 | 6 прямых вызовов `ai.models.generateContent()` из браузера — AI-запросы идут напрямую с клиента, минуя сервер; невозможен аудит, логирование, rate-limit |
| 4 | `server.ts` | 257–763 | Ни один из 10 API-эндпоинтов (`/api/data`, `/api/orders/status`, `/api/inventory/*`, `/api/admin/*`, `/api/comments/reply`, `/api/reports/save`, `/api/telegram-report`) **не имеет middleware аутентификации** — любой желающий может читать все данные и совершать мутации |
| 5 | `server.ts` | 25 (package.json) | `express-rate-limit` установлен в `package.json` но **нигде не применяется** — любой эндпоинт открыт для неограниченного перебора и DoS |

---

## 🔐 Безопасность (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `index.html` | 8 | Нет `<meta http-equiv="Content-Security-Policy">` — открыт XSS через `innerHTML`, `eval`, вставку сторонних скриптов |
| 2 | `index.html` | 8 | `<script src="https://telegram.org/js/telegram-web-app.js">` без атрибута `integrity` (SRI) — при компрометации CDN загружается вредоносный скрипт без предупреждения |
| 3 | `server.ts` | 447–488 | `/api/telegram-report` принимает `reportText` от клиента и передаёт его **напрямую** в Telegram сообщение с `parse_mode: 'HTML'` без какой-либо санитизации — HTML-инъекция в чаты администраторов |
| 4 | `src/App.tsx` | 1141–1210 | Аутентификация реализована **только на клиенте** (`role`, `staffName` хранятся в `useState`) — нет серверной сессии, нет JWT, нет cookie; при прямых API-запросах сервер принимает всё |

---

## 🐛 Баги и логические ошибки (14)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `src/App.tsx` | 2476 | `const result = JSON.parse(response.text)` без `try/catch` — Gemini часто возвращает markdown-блок ` ```json ... ``` `, что вызывает `SyntaxError` и падение всего обработчика |
| 2 | `src/App.tsx` | 513 | `+{Math.round(Math.random() * 5)}%` прямо в JSX — новое случайное значение на **каждый рендер**, создаёт визуальное мигание при любом обновлении стейта |
| 3 | `src/App.tsx` | 1494–1500 | `Math.random()` в `useMemo` для `chartData` (`prevOrders`, `prevRevenue`, `forecast`) — пересчитывается каждый раз при изменении `orders`, данные "прыгают" |
| 4 | `src/App.tsx` | 1528–1533 | `Math.random()` в `useMemo` для `popularityData` (`growth`, `margin`, `value`) — при каждом рендере разные значения, гистограммы мерцают |
| 5 | `src/App.tsx` | 5247 | `{Math.round(Math.random() * 5 + 5)}%` в JSX AI-комментарии — каждый рендер показывает другой процент "роста операционной эффективности" |
| 6 | `src/App.tsx` | 692 | `setShiftHistory(JSON.parse(storedHistory))` без `try/catch` — при повреждённом значении в `localStorage` (ручная правка, ошибка браузера) приложение падает с белым экраном |
| 7 | `src/App.tsx` | 921–922 | `JSON.parse(stored)` для оффлайн-продаж без `try/catch` — аналогичный краш при неверном JSON в `localStorage` |
| 8 | `src/App.tsx` | 2363 | `showToast` в массиве зависимостей `useEffect` (строка `[orders, notifiedPickupOrders, showToast]`) — `showToast` — обычная функция (не `useCallback`), пересоздаётся на каждый рендер, вызывая бесконечный цикл обновлений |
| 9 | `src/App.tsx` | 1028–1032 | Сортировка `monthlyAggregated` по полю `month` через `localeCompare` — функция всегда возвращает `0` для одинаковых строк; реальный порядок месяцев не гарантирован |
| 10 | `src/App.tsx` | 1632 | `topSellers` сортируется по `stock` (текущий остаток) вместо `salesCount` или `revenue` — показывает товары с наибольшим запасом, а не самые продаваемые |
| 11 | `src/App.tsx` | 1642 | `soldItems: todaySalesCount * 3` — искусственный множитель `* 3` без бизнес-логики; данные по проданным позициям некорректны |
| 12 | `src/App.tsx` | 1958 | `price: parseInt(newItem.price)` — `parseInt` обрезает дробную часть; цены вида `12.50` превращаются в `12`; нужен `parseFloat` |
| 13 | `src/App.tsx` | 4862 | `ID: {Math.random().toString(36).substr(2, 9).toUpperCase()}` прямо в JSX — новый ID на каждый рендер; если ID используется как ключ React, постоянный remount |
| 14 | `src/types.ts` | 54–56 | `Comment.replied: boolean` и `Comment.replyText: string` определены в типе, но `/api/data` их **не возвращает** — поля всегда `undefined`/`false` после обновления страницы; ответы теряются |

---

## 🎨 UI/UX проблемы (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `src/App.tsx` | 1781 | `confirm("Вы уверены, что хотите удалить этот товар из базы?")` — `window.confirm()` **заблокирован** в Telegram WebApp и в iframe; кнопка удаления молча возвращает `false`, товар не удаляется без объяснений |
| 2 | `src/App.tsx` | 6574 | `"Уровень 12"` хардкод — уровень пользователя не рассчитывается из реальных данных |
| 3 | `src/App.tsx` | 6578 | `"топ-3% администраторов"` — полностью выдуманный маркетинговый текст, не связанный с реальной статистикой |
| 4 | `src/App.tsx` | 6684 | `"Сессии (4)"` — хардкод количества сессий; реальный счётчик не реализован |

---

## 📦 Проблемы с данными (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `src/constants.ts` | 1–31 | `DAILY_STATS` содержит **фиктивные placeholder-данные** (8–92 продажи/час) и используется как fallback на дашборде; при сбое загрузки пользователь видит правдоподобные, но ненастоящие цифры |
| 2 | `src/constants.ts` vs `server.ts` | — | `DAILY_STATS` продублирован в двух местах с **разным количеством слотов**: 7 часов в `constants.ts` против 13 часов в `server.ts`; данные несовместимы |
| 3 | `server.ts` | 153, 270, 458, 509, 572, 605, 624, 671, 688, 751 | `doc.loadInfo()` вызывается **10 раз** независимо, без кеширования — каждый API-запрос инициирует отдельный HTTP-запрос к Google Sheets API; при параллельных запросах — 10× нагрузка |
| 4 | `src/App.tsx` | 651–654 | `INITIAL_STAFF` и `INITIAL_SHIFT_REPORTS` содержат хардкодированные имена сотрудников и даты (`"Аня Синицкая"`, `"Катя Мороз"`, `"2024-11-15"`) — попадают в продакшн |

---

## 🚀 Деплой и сборка (5)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `package.json` | 12, 22 | `vite` присутствует одновременно в `dependencies` **и** `devDependencies` — Railway устанавливает его дважды, раздувает образ |
| 2 | `package.json` | 7 | `"start": "tsx server.ts"` — `tsx` — это dev-транспайлер на базе esbuild, не предназначенный для продакшна; для продакшна нужен `tsc` + `node dist/server.js` или аналог |
| 3 | `package.json` | — | Отсутствует `nixpacks.toml` для Railway — платформа сама угадывает версию Node.js; возможны несовпадения окружения между dev и prod |
| 4 | `tsconfig.json` | — | Нет `"strict": true` — TypeScript не проверяет `null`/`undefined`, неявные `any`, лишние параметры; типобезопасность частичная |
| 5 | `.env.example` | — | Отсутствуют: `TELEGRAM_CLIENT_CHAT_ID` (используется `server.ts:93`), `READ_ONLY` (используется в 6 местах), `PORT` (используется `server.ts:19`) |

---

## 💀 Мёртвый код (6)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `fix.ts` | 1–EOF | Одноразовый патч-скрипт в корне проекта. **Опасность:** последняя строка `newLines.join('\\n')` использует экранированный `\n` вместо реального символа переноса строки — при запуске перезапишет `App.tsx`, заменив все `\n` на буквальную строку `"\n"`, что сломает файл |
| 2 | `fix_join_newlines.js` | — | Одноразовый utility-скрипт, должен быть удалён после использования |
| 3 | `restore_newlines.js` | — | Одноразовый utility-скрипт, должен быть удалён |
| 4 | `app/applet/resize.js` | — | Одноразовый Jimp-скрипт для ресайза изображений (`require('jimp')`), оставлен в проекте; использует CommonJS require |
| 5 | `package.json` | 18 | `"jimp": "^1.6.0"` — зависимость установлена, используется только в `app/applet/resize.js` (одноразовый скрипт) |
| 6 | `package.json` | 19 | `"googleapis": "^146.0.0"` — установлена, но нигде не импортируется в CRM (используется прямо Google Sheets API через `google-spreadsheet`) |

---

## 📝 Архитектура (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `src/App.tsx` | 1–6781 | **God Component** — 6 781 строка в одном файле: весь стейт (60+ `useState`), вся бизнес-логика, все вычисления (`useMemo`), весь UI. Невозможно тестировать, поддерживать, онбордить новых разработчиков |
| 2 | `server.ts` | — | Нет паттерна аутентификации как такового — логин реализован только на клиенте; сервер не знает, кто делает запросы; невозможно внедрить RBAC без полного рефакторинга |
| 3 | `src/main.tsx` | 1–30 | Неполные TypeScript-декларации для Telegram WebApp API: отсутствуют `initData`, `initDataUnsafe`, `platform`, `colorScheme`, `themeParams` — работа с Mini App "вслепую" без типов |
| 4 | — | — | Нет ни одного теста: ни unit-, ни интеграционных, ни E2E — при любом рефакторинге нет страховки |

---

---

# ПРОЕКТ 2: `maminhleb_bot`

**Стек:** Node.js + JavaScript (без TypeScript) + Telegram Bot API + Google Sheets API (`googleapis`) + Express.js + node-cron  
**Деплой:** Railway.app  
**Функции:** Telegram-бот для приёма заказов, Happy Hour рассылка, Mini App сервер, управление складом

---

## 🔴 Критические проблемы (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 17 | `res.header('Access-Control-Allow-Origin', '*')` — CORS wildcard на **всех** эндпоинтах включая мутирующие (`/order`, `/reply`) — любой сайт из браузера пользователя может делать кросс-доменные запросы |
| 2 | `bot/index.js` | 932 | `app.post('/order', ...)` — нет аутентификации; любой знающий URL может создать фейковый заказ, который попадёт в Google Sheets и вызовет уведомление администратору |
| 3 | `bot/index.js` | 792 | `app.post('/reply', ...)` — нет аутентификации; принимает `{ chat_id, text }` от клиента и отправляет сообщение в **любой** Telegram-чат; возможно спамирование от имени бота |
| 4 | `bot/index.js` | 949 | `app.get('/api/test-sheets', ...)` — публичный диагностический эндпоинт, который **записывает тестовую строку в продакшн-таблицу** Google Sheets; открыт без авторизации |

---

## 🔐 Безопасность (3)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 17–18 | `Access-Control-Allow-Origin: *` + `Access-Control-Allow-Headers: Content-Type` — wildcard CORS позволяет CSRF-атаки и эксфильтрацию данных через браузеры пользователей |
| 2 | `bot/index.js` | 792–819 | `/reply` эндпоинт отправляет сообщения по `chat_id` из тела запроса без валидации — нет проверки что `chat_id` принадлежит пользователю бота; атакующий может слать сообщения в любой чат |
| 3 | `bot/index.js` | 932 | `/order` принимает заказы без проверки `initData` от Telegram WebApp — нет HMAC-верификации подписи, предусмотренной Telegram Bot API |

---

## 🐛 Баги и логические ошибки (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 133–149 | **Race condition в `appendRow`**: две одновременные заявки оба читают `A:A` → получают одинаковый `nextRow` → пишут в одну строку; один заказ затирает другой |
| 2 | `bot/index.js` | 871–913 | **Race condition в `decrementStock`**: не-атомарное `read → modify → write` — при двух одновременных заказах остаток декрементируется только один раз вместо двух |
| 3 | `bot/index.js` | 122–132 | `getSheets()` кеширует экземпляр `sheetsApi` навсегда (`if (sheetsApi) return sheetsApi`) — OAuth-токен Google истекает примерно через 1 час; после этого все API-вызовы падают с `401` до перезапуска процесса |
| 4 | `bot/index.js` | 1109 | Ключи дедупликации сообщений (`setProp(msgKey, '1')`) никогда не удаляются из `state.json` — файл растёт бесконечно; при большой нагрузке сотни тысяч ключей замедлят работу |

---

## 🎨 UI/UX (1)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 16–19 | Mini App открывается через `sendFile(path.join(__dirname, 'index.html'))` — нет обработки ошибок загрузки ресурсов; при недоступности `/api/stock` пользователь видит пустой экран без какого-либо feedback |

---

## 📦 Проблемы с данными (3)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 39 | `const STATE_FILE = './state.json'` — файловая персистентность на Railway с **эфемерной** файловой системой; `state.json` со счётчиками заказов, флагами рассылки и дедупликационными ключами **уничтожается при каждом деплое или перезапуске** |
| 2 | `bot/index.js` | 324–329 | `known_clients` собирается (парсится из Sheets, обновляется в `state.json`), но никогда не используется — Happy Hour рассылка читает клиентов прямо из столбца F Sheets; `known_clients` — утечка данных клиентов без пользы |
| 3 | `bot/index.js` | 73–120 | Счётчик заказов (`orderCount`) инициализируется при старте из Google Sheets, но пересчитывается по строкам; при race condition (п. 🐛1) счётчик рассинхронизируется со Sheets |

---

## 🚀 Деплой и сборка (3)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 39 | `state.json` как хранилище состояния несовместим с Railway (эфемерная FS) — нужен внешний KV-хранилище (Redis, Upstash) или та же Google Sheets |
| 2 | `bot/.env.example` | — | Отсутствуют: `DELIVERY_CHAT_ID` (используется `index.js:25`), `PREORDER_CHAT_ID` (используется `index.js:26`), `ORDERS_SHEET_NAME` (используется `index.js:30`), `PORT` (используется `index.js:31`) |
| 3 | `bot/package.json` | — | Нет TypeScript — нет типобезопасности, нет IDE-помощи, ошибки типов обнаруживаются только в рантайме |

---

## 💀 Мёртвый код (2)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 324–329 | `known_clients` — собирается, сохраняется в `state.json`, но нигде не читается для функциональности (только перезаписывается) |
| 2 | `bot/package.json` | — | `"node-fetch": "^2.7.0"` — CommonJS пакет v2; Node.js 18+ имеет нативный `fetch`; зависимость не нужна |

---

## 📝 Архитектура (4)

| # | Файл | Строка | Проблема |
|---|------|--------|----------|
| 1 | `bot/index.js` | 1–1195 | **Монолит 1195 строк**: Telegram-бот + Google Sheets интеграция + обработка заказов + Mini App Express-сервер + node-cron Happy Hour — всё в одном файле без разделения ответственности |
| 2 | `bot/index.js` | — | Нет TypeScript — при добавлении новых полей в структуру заказа (`appendRow`) нет проверки количества и типа аргументов; рассинхронизация строки Sheets и кода не обнаруживается |
| 3 | — | — | Нет тестов — логика `appendRow`, `decrementStock`, обработчики команд не покрыты |
| 4 | `bot/index.js` | 122 | `getSheets()` — singleton без механизма обновления токена; архитектурно нужен `googleapis` с `auth.refreshAccessToken()` или автообновление через `GoogleAuth` |

---

---

# 🌐 Кросс-проектные проблемы (5)

| # | Категория | Проекты | Проблема |
|---|-----------|---------|----------|
| 1 | 🔐 Безопасность | CRM + Bot | Оба проекта **не аутентифицируют API-запросы** от Mini App — CRM-сервер принимает всё без сессий, Bot принимает заказы без HMAC-верификации; единый Auth layer отсутствует |
| 2 | 📦 Данные | CRM + Bot | **Google Sheets как единственная БД** без резервного копирования, без транзакций, с лимитом Google API (300 запросов/мин/проект); при блокировке аккаунта весь бизнес останавливается |
| 3 | 📦 Данные | CRM + Bot | **Нет общих типов/контрактов** для структуры заказа — CRM разбирает строки Sheets по позиции (A1, B1...), Bot пишет их в том же порядке; изменение схемы в одном месте ломает другое без compile-time ошибки |
| 4 | 🚀 Деплой | CRM + Bot | **Нет request logging/monitoring** (Morgan, Pino, Datadog, Sentry) — при ошибке в продакшне нет трассировки, нет алертов; обнаружение проблем происходит только когда клиент жалуется |
| 5 | 🚀 Деплой | CRM + Bot | **Нет CI/CD**: нет `.github/workflows/` — код пушится без автопроверок линтера, типов, тестов; сломанная сборка попадает в прод |

---

---

# 🚀 Рекомендации по будущим фичам

> Технический долг закрыт → что строить дальше

## Приоритет 1 — Безопасность (должно быть до публичного запуска)

1. **JWT-аутентификация** — серверные сессии с `jsonwebtoken` + HttpOnly cookie; роли Baker/Cashier/Chef/Admin хранятся в токене; все эндпоинты защищены `requireAuth` middleware
2. **Telegram initData HMAC** — верификация `window.Telegram.WebApp.initData` на сервере для всех Mini App запросов
3. **AI Proxy endpoint** — перенести все 6 Gemini-вызовов из браузера на сервер (`/api/ai/insights`, `/api/ai/report` и т.д.); убрать `GEMINI_API_KEY` из Vite `define`
4. **Rate Limiting** — применить `express-rate-limit` с разными лимитами: `/api/login` 5 req/15min, `/api/ai/*` 10 req/min, прочие эндпоинты 100 req/min
5. **Google Sheets cache** — добавить `ensureSheetsLoaded()` singleton с TTL 5 минут; убрать 10 дублирующих `doc.loadInfo()`

## Приоритет 2 — Надёжность

6. **Redis/Upstash** — заменить `state.json` для Bot на Redis (Upstash Free tier); убрать зависимость от Railway ephemeral FS
7. **Atomic row writing** — использовать Google Sheets `batchUpdate` с `appendCells` (атомарная операция) вместо read-then-write в `appendRow`; устранит race condition
8. **Error boundaries** — обернуть каждую вкладку CRM в `<ErrorBoundary>`; при краше одного блока остальное приложение продолжает работать
9. **Sentry / Telegram Error Alerts** — интегрировать error reporting; при необработанном исключении на сервере — сообщение в admin-чат

## Приоритет 3 — Качество кода

10. **Декомпозиция App.tsx** — разбить на: `hooks/useOrders.ts`, `hooks/useInventory.ts`, `hooks/useShift.ts`, `components/Dashboard/`, `components/Orders/`, `components/Inventory/`; God Component в 6781 строку — технический риск №1
11. **TypeScript в Bot** — мигрировать `bot/index.js` → `bot/src/index.ts`; добавить `tsconfig.json`; типизировать структуру заказа, строки Sheets, события бота
12. **Shared types** — создать `shared/types.ts` как git submodule или отдельный npm-пакет; единая `OrderRow` для CRM и Bot
13. **Тесты** — минимум: unit-тесты для `appendRow`, `decrementStock`, парсинга заказов; интеграционный тест для `/api/data`

## Приоритет 4 — Функциональность

14. **Аналитика с реальными данными** — заменить `Math.random()` в `chartData` и `popularityData` на реальные расчёты из исторических заказов
15. **Offline-first** — добавить Service Worker + IndexedDB для работы CRM при нестабильном интернете (особенно актуально для Telegram Mini App)
16. **Push-уведомления** — Happy Hour через Telegram Mini App notifications API вместо файлового флага в `state.json`
17. **Аудит логи заказов** — отдельная вкладка в CRM: кто и когда менял статус заказа; хранить в Sheets или отдельном листе
18. **WebSocket / SSE** — real-time обновления статусов заказов без polling; при смене статуса в боте — CRM обновляется автоматически

---

---

# 📊 Итоговая статистика

| Категория | CRM (`crm-maminhleb7`) | Bot (`maminhleb_bot`) | Cross-project | Итого |
|-----------|----------------------|----------------------|---------------|-------|
| 🔴 Критические | 5 | 4 | — | **9** |
| 🔐 Безопасность | 4 | 3 | 1 | **8** |
| 🐛 Баги/Логика | 14 | 4 | — | **18** |
| 🎨 UI/UX | 4 | 1 | — | **5** |
| 📦 Данные | 4 | 3 | 2 | **9** |
| 🚀 Деплой/Сборка | 5 | 3 | 2 | **10** |
| 💀 Мёртвый код | 6 | 2 | — | **8** |
| 📝 Архитектура | 4 | 4 | — | **8** |
| **Итого** | **46** | **24** | **5** | **75** |

---

## Критический путь (исправить первыми)

1. `vite.config.ts:11` → убрать `GEMINI_API_KEY` из `define`, перенести AI на сервер
2. `server.ts` → добавить `requireAuth` middleware на все эндпоинты кроме `/api/health` и `/api/telegram/webhook`
3. `bot/index.js:17` → ограничить CORS до `https://*.railway.app` и домена Mini App
4. `bot/index.js:932` → `/order` — верифицировать Telegram `initData` перед сохранением заказа
5. `bot/index.js:949` → удалить или защитить `/api/test-sheets`
6. `server.ts` → применить `express-rate-limit` на все эндпоинты
7. `src/App.tsx:2476` → обернуть `JSON.parse(response.text)` в `try/catch`; стрипать markdown-блок перед парсингом
8. `bot/index.js:133` → заменить `appendRow` на атомарный `appendCells` Google Sheets API

---

*Аудит проведён на коммите `cf5e4907` (crm-maminhleb7) и `53e7a9b` (maminhleb_bot) ветки `claude/audit-white-fox-dashboard-W4DAS`*  
*Дата: 17 мая 2026*
